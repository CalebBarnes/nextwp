{
  "private": true,
  "packageManager": "pnpm@8.14.1",
  "scripts": {
    "dev": "turbo run dev --filter='nextwp-starter' --log-prefix=none",
    "dev:docs": "turbo run dev --filter='nextwp-docs'",
    "build": "turbo run build --filter='nextwp-starter'",
    "start": "turbo run start",
    "stackbit-dev": "pnpm --filter nextwp-starter stackbit-dev"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
