{
  "private": true,
  "scripts": {
    "dev": "turbo run dev --scope='nextwp-starter' --log-prefix=none",
    "dev:docs": "turbo run dev --scope='nextwp-docs'",
    "build": "turbo run build --scope='nextwp-starter'",
    "start": "turbo run start",
    "stackbit-dev": "pnpm --filter nextwp-starter stackbit-dev"
  }
}
