import Link from 'next/link'

export const metadata = {
  title: 'create-nextwp-app',
  description:
    'A CLI tool that allows you to create a new NextWP project with a single command.',
}

# create-nextwp-app

<div className="not-prose my-5 flex gap-3">
  <Link href="https://www.npmjs.com/package/create-nextwp-app" target="_blank">
    {/* eslint-disable-next-line @next/next/no-img-element */}
    <img src="https://badge.fury.io/js/create-nextwp-app.svg" alt="npm" />
  </Link>
</div>

A CLI tool that allows you to create a new NextWP project with a single command.

## Usage

<CodeGroup>

```bash {{ title: "pnpm" }}
pnpx create-nextwp-app
```

```bash {{ title: "yarn" }}
yarn create nextwp-app
```

```bash {{ title: "npm" }}
npx create-nextwp-app
```

</CodeGroup>

<Note>@todo: Add usage instructions</Note>
