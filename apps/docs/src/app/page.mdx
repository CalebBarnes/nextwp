import { Guides } from '@/components/Guides'
import { Resources } from '@/components/Resources'
import { HeroPattern } from '@/components/HeroPattern'

export const metadata = {
  title: 'NextWP - Documentation',
  description:
    'Learn everything there is to know about the Protocol API and integrate Protocol into your product.',
}

export const sections = [
  { title: 'Guides', id: 'guides' },
  { title: 'Resources', id: 'resources' },
]

<HeroPattern />

# NextWP Documentation

Learn everything there is to know about building Headless WordPress sites with NextWP. {{ className: 'lead' }}

<div className="not-prose mb-16 mt-6 flex gap-3">
  <Button href="/quickstart" arrow="right">
    <>Quickstart</>
  </Button>
  <Button href="/packages" variant="outline">
    <>Explore Packages</>
  </Button>
</div>

## Getting started {{ anchor: false }}

To get started, you need a WordPress site.
You can use an existing site or create a new one. Some features require the [NextWP Toolkit](#) plugin installed in WordPress. <br/>
The easiest way to prepare your WordPress site for NextWP is to install the [NextWP Headless Theme](#)
to automatically install the recommended plugins. {{ className: 'lead' }}

<Guides />

<Resources />
